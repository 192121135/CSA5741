#include<stdio.h>
int main() {
   int x, y, temp;
   printf("Enter the value of x and y: ");
   scanf("%d %d", &x, &y);
   printf("Before swapping x=%d, y=%d ", x, y);
   temp = x;
   x = y;
   y = temp;
   printf("After swapping x=%d, b=%d", x, y);
   return 0; 
}

OUTPUT:
Enter the value of x and y: 20
10
Before swapping x=20, y=10 After swapping x=10, b=20
